# Placeholder so `//go:embed all:dist` in webui.go compiles in a plain checkout.
# `make web` (or the Docker build) replaces this directory with the real SPA build.
# Do not delete: without it `go build ./cmd/server` fails with "no matching files".
